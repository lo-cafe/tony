import styled from 'styled-components';

const Title = styled.div`
  margin: 0;
  margin-top: 16px;
  font-size: 20px;
  margin-bottom: 8px;
  font-family: inherit;
  font-weight: 700;
`;

export default Title
